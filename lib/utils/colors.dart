import 'package:flutter/material.dart';

class AppColors {
  // primary colors
  static const Color primary = Color(0xFF4F378B);
  static const Color primaryLight = Color(0xFF7C5DC7);

  // secondary colors
  static const Color secondary = Color(0xFFF7B801);
  static const Color secondaryLight = Color(0xFFFFD55D);
  static const Color secondaryDark = Color(0xFFE55A2B);

  // Accent colors
  static const Color accent = Color(0xFFE85D04);
  static const Color accentLight = Color(0xFFFFCA5A);

  // Neutral colors
  static const Color background = Color(0xFFF8F8F8);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color cardBackground = Color(0xFFFFFFFF);

  // Text colors
  static const Color textPrimary = Color(0xFF22223B);
  static const Color textSecondary = Color(0xFF9A9A9A);
  static const Color textLight = Color(0xFFBDBDBD);

  // Status colors
  static const Color success = Color(0xFF28A745);
  static const Color error = Color(0xFFFFC107);

  // border & Divider colors
  static const Color border = Color(0xFFDEE2E6);
  static const Color divider = Color(0xFFE9ECEF);
}